## Packages
framer-motion | Smooth animations for result reveals and interactions
lucide-react | Beautiful icons for UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
- Images are handled via local state/preview (URL.createObjectURL)
- Backend mock returns static unsplash image for 'after' view
- Font families: 'Outfit' for headings, 'DM Sans' for body text
